<%# One entry in the page's shared counterparty datalist.  Its own partial because TransactionsController
    appends a single one of these when a save creates a counterparty. %>
<option value="<%= counterparty.name %>"></option>
