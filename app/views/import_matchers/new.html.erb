<% content_for :title, "New rule" %>

<h1>New rule for <%= @account.name %></h1>

<%= form_actions back: account_import_matchers_path(@account) %>

<%= render "form", import_matcher: @import_matcher, account: @account %>
