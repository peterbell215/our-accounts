<% content_for :title, "Editing rule" %>

<h1>Editing rule for <%= @account.name %></h1>

<%= render "form", import_matcher: @import_matcher, account: @account %>

<br>

<div>
  <%= link_to "Back to rules", account_import_matchers_path(@account) %>
</div>
